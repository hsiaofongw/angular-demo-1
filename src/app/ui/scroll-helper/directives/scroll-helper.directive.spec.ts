import { ScrollHelperDirective } from './scroll-helper.directive';

describe('ScrollHelperDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollHelperDirective();
    expect(directive).toBeTruthy();
  });
});
